{
  "name": "react-upload-control",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/core && npm run build",
    "dev": "cd packages/core && npm run start",
    "test": "cd packages/core && npm run test",
    "lint": "cd packages/core && npm run lint",
    "storybook": "cd packages/core && npm run storybook",
    "build-storybook": "cd packages/core && npm run build-storybook"
  },
  "devDependencies": {
    "@rollup/plugin-babel": "^6.0.4",
    "@rollup/plugin-commonjs": "^28.0.2",
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-typescript": "^12.1.2",
    "rollup-plugin-terser": "^7.0.2",
    "rollup-plugin-visualizer": "^5.14.0"
  }
}
